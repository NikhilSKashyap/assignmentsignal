[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m assignment.relay.server"
healthcheckPath = "/healthz"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/data"
